# Neural Network Efficiency Repository

This repository contains multiple final-year project submodules.

## Subprojects

- `EDUSYNC` — A React/Firebase educational dashboard project.
- `mnist-granger-pruning` — A neural network pruning research project.

Each subfolder is self-contained. See the README inside each project folder for details.
